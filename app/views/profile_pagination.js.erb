$('#feed').append('<%= j render @profiles %>');
<% if @profiles.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @profiles %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
